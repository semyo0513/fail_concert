# 우당탕탕 AI 융합교육 생존기 — 웹앱 개발 계획서

> 교사 연수 네트워킹 시간에 활용하는 실패 이력서 작성 + 실시간 시상 투표 시스템

---

## 1. 프로젝트 개요

### 배경 및 목적

AI 융합교육을 실천하는 교사들은 기술적 실패와 예기치 못한 변수를 반복적으로 경험한다. 이 경험은 대개 혼자 삭이고 사라지지만, 사실상 동료들과 공유할 때 가장 강력한 학습 자원이 된다. 본 웹앱은 교사 연수 네트워킹 세션에서 이 '화려한 실패'를 공식적으로 기록하고, 모둠 내에서 공유하고, 전체 투표를 통해 시상하는 일련의 흐름을 지원한다.

### 핵심 가치

- **심리적 안전감**: 실패를 부끄러운 것이 아닌 지적 자산으로 재프레이밍
- **집단 지성**: 개인의 삽질이 모둠의 노하우가 되고, 전체의 인사이트가 되는 구조
- **즉시 활용 가능**: 연수 현장에서 QR 하나로 입장, 별도 앱 설치 없이 모바일로 참여

### 운영 시나리오 (전체 흐름)

```
[단계 1] 실패 이력서 작성 (개인, 15분)
    ↓
[단계 2] 모둠 공유 보드 (모둠, 10분)
    ↓
[단계 3] 시상 투표 (관리자 오픈 → 전체 참가자 투표, 5~10분)
    ↓
[단계 4] 최종 시상식 결과 발표
```

---

## 2. 사용자 역할 및 화면 구성

### 역할 분류

| 역할 | 설명 | 접근 방법 |
|---|---|---|
| **참가자** | 실패 이력서를 작성하고 투표하는 교사 | 세션 QR / URL + 참가자 코드 입장 |
| **관리자(퍼실리테이터)** | 세션 생성, 단계 진행 제어, 투표 오픈·마감, 시상 선정 | 별도 관리자 로그인 또는 세션 생성 키 |

### 화면 목록

| 화면 ID | 화면명 | 역할 |
|---|---|---|
| `S-01` | 입장 화면 | 참가자 |
| `S-02` | 실패 이력서 작성 (파트 1~3) | 참가자 |
| `S-03` | 모둠 공유 보드 | 참가자 |
| `S-04` | 시상 투표 화면 | 참가자 |
| `S-05` | 시상 결과 화면 | 참가자 |
| `A-01` | 관리자 대시보드 | 관리자 |
| `A-02` | 투표 관리 및 결과 모니터링 | 관리자 |
| `A-03` | 대형 화면 투사용 결과 화면 | 관리자 (전체 투사) |

---

## 3. 기능 명세

### 3-1. 입장 화면 (S-01)

- QR코드 또는 URL로 접속
- 이름(또는 닉네임) + 소속 학교 입력
- 세션 코드 입력으로 모둠 자동 배정 (예: 같은 코드 입력 시 동일 모둠)
- 세션 상태 확인 (대기 중 / 작성 중 / 투표 중 / 마감)

### 3-2. 실패 이력서 작성 (S-02)

3개 파트로 구성된 스텝 UI. 각 파트 완료 후 다음 파트로 전환.

#### 파트 1 — 사건 재구성

| 항목 | 설명 |
|---|---|
| 수업/프로젝트명 | 단문 입력 (최대 50자) |
| 사건 재구성 | 자유 서술, 최소 50자 권장, 글자 수 실시간 표시 |
| 자동 임시저장 | 30초마다 localStorage 저장 (새로고침 복원) |

#### 파트 2 — 사망 진단서 (5 Whys)

| 항목 | 설명 |
|---|---|
| 표면적 원인 | 기술적 오류 등 즉각적 이유 |
| 5 Whys 드릴다운 | "왜?" 버튼 클릭 시 한 단계씩 추가 입력 필드 생성 (최대 5단계) |
| 원인 카테고리 태그 | 환경(와이파이·기기) / 준비 부족 / 기술 오류 / 학생 변수 / 기타 (중복 선택 가능) |

**5 Whys UI 동작 방식:**

```
표면 원인: [와이파이가 끊겼다]
  └─ 왜? [공유기 채널이 혼잡했다]
       └─ 왜? [사전에 네트워크 테스트를 안 했다]
            └─ 왜? [수업 준비 시간이 부족했다]
                 └─ 왜? 버튼 (여기서 멈추거나 계속)
```

#### 파트 3 — 플랜 B 설계 (AAR 회고)

| 항목 | 설명 |
|---|---|
| 배운 점 | 이 실패를 통해 새롭게 깨달은 것 |
| 아날로그 대안 | 같은 상황 재발 시 꺼낼 수 있는 우회 경로 |
| AI 피드백 (선택) | "비슷한 상황에서 다른 선생님들의 플랜 B 예시 보기" 버튼 → Claude API 호출 |

**Claude API 호출 프롬프트 예시:**

```
사용자가 입력한 실패 상황: {파트1 내용}
표면 원인: {파트2 표면 원인}
카테고리: {선택한 태그}

위 상황에서 실제 교사들이 활용할 수 있는 현실적인 아날로그 대안 또는 
플랜 B를 3가지 제시해 주세요. 각 방안은 2~3문장으로 간결하게.
```

### 3-3. 모둠 공유 보드 (S-03)

- 같은 세션 코드로 입장한 참가자들의 이력서 카드가 모둠 보드에 자동 집계
- 카드는 기본적으로 앞면(이름 + 수업명)만 노출, 클릭 시 뒤집어서 전체 내용 확인
- 공감 버튼(익명): 카드당 누적 공감 수 표시
- 한 줄 댓글: 50자 이내 익명 메시지 남기기
- 원인 카테고리 분포 미니 차트: 모둠 내 가장 많이 선택된 태그 시각화

### 3-4. 시상 투표 화면 (S-04)

> **관리자가 투표를 오픈해야만 이 화면으로 진입 가능**

#### 시상 카테고리 (3종)

| 시상 이름 | 설명 |
|---|---|
| 올해의 아름다운 폭망상 | 가장 혁신적인 시도를 한 사례 |
| 불사조상 | 멘붕 상황을 가장 기가 막히게 수습한 사례 |
| 인간 디버거상 | 가장 처절하게 오류와 싸운 사례 |

#### 투표 UI 동작

1. 투표 화면 진입 시 모든 참가자의 이력서 카드 목록이 표시됨
2. 각 카테고리별로 1명씩 선택 (중복 불가, 자기 자신 투표 불가)
3. "투표 완료" 버튼 클릭 → 제출 (수정 불가)
4. 투표 완료 후 대기 화면: 진행 상황 표시 ("전체 N명 중 M명 투표 완료")

#### 관리자 투표 제어

- 관리자 대시보드(A-01)에서 "투표 오픈" 버튼 클릭 → 참가자 화면이 S-04로 전환
- 투표 진행 중 실시간 참여율 모니터링 (A-02)
- "투표 마감" 버튼 클릭 → 신규 투표 차단, 결과 집계

### 3-5. 시상 결과 화면 (S-05 / A-03)

- 카테고리별 1위 수상자 카드를 순차적으로 공개 (관리자가 "공개" 버튼 클릭 시)
- 수상자 이력서 전체 내용을 대형 화면에 풀스크린으로 표시
- 공동 1위 처리: 동점 시 두 카드 동시 공개
- 참가자 화면에도 실시간 동기화로 결과 표시

---

## 4. 관리자 기능 상세 (A-01 / A-02)

### 세션 생성 및 관리

```
세션 생성 시 설정 항목:
  - 연수명 (예: 2025 삼현 AI 연수 2기)
  - 날짜
  - 모둠 수 및 모둠별 세션 코드 (자동 생성 또는 직접 입력)
  - 시상 카테고리 활성화 여부 (기본 3종, 추가·삭제 가능)
```

### 단계 진행 제어판

| 버튼 | 동작 |
|---|---|
| 이력서 작성 시작 | 참가자 화면 S-02 활성화 |
| 모둠 공유 오픈 | 참가자 화면 S-03 활성화 |
| **투표 오픈** | 참가자 화면 S-04 활성화 (핵심 기능) |
| **투표 마감** | 신규 투표 차단, 결과 집계 시작 |
| 결과 공개 (카테고리별) | S-05 순차 공개 |

### 실시간 모니터링

- 이력서 작성 완료율 (N명 / 전체 M명)
- 투표 참여율 실시간 바 차트
- 카테고리별 득표 현황 (투표 마감 전까지는 관리자에게만 표시)

---

## 5. 데이터 구조

### 세션 (Session)

```json
{
  "sessionId": "string",
  "title": "string",
  "date": "ISO8601",
  "status": "waiting | writing | sharing | voting | closed",
  "groups": [
    { "groupCode": "string", "groupName": "string" }
  ],
  "awardCategories": [
    { "id": "string", "name": "string", "description": "string" }
  ]
}
```

### 실패 이력서 (FailureCV)

```json
{
  "cvId": "string",
  "sessionId": "string",
  "groupCode": "string",
  "authorName": "string",
  "school": "string",
  "createdAt": "ISO8601",
  "part1": {
    "projectName": "string",
    "story": "string"
  },
  "part2": {
    "surfaceCause": "string",
    "fiveWhys": ["string"],
    "categories": ["env | prep | tech | student | other"]
  },
  "part3": {
    "lesson": "string",
    "planB": "string"
  },
  "reactions": {
    "empathy": "number",
    "comments": [{ "text": "string", "createdAt": "ISO8601" }]
  }
}
```

### 투표 (Vote)

```json
{
  "voteId": "string",
  "sessionId": "string",
  "voterId": "string (익명 해시)",
  "selections": [
    { "categoryId": "string", "cvId": "string" }
  ],
  "submittedAt": "ISO8601"
}
```

### 시상 결과 (AwardResult)

```json
{
  "sessionId": "string",
  "results": [
    {
      "categoryId": "string",
      "categoryName": "string",
      "winners": [
        { "cvId": "string", "authorName": "string", "votes": "number" }
      ]
    }
  ]
}
```

---

## 6. 기술 스택

### 프론트엔드

| 항목 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | React (Vite) | 컴포넌트 기반, 상태 관리 용이 |
| 스타일링 | Tailwind CSS | 빠른 UI 구성, 모바일 반응형 |
| 상태 관리 | Zustand | 가볍고 직관적 |
| 라우팅 | React Router v6 | SPA 화면 전환 |
| PWA | vite-plugin-pwa | 모바일 홈 화면 추가, 오프라인 임시저장 |

### 백엔드 / 실시간

| 항목 | 선택 | 이유 |
|---|---|---|
| 실시간 DB | **Firebase Realtime DB** 또는 **Supabase** | 참가자 화면 실시간 동기화, 투표 결과 즉시 반영 |
| 인증 | Firebase Auth (익명 로그인) | 설치 없이 참가자 식별, 중복 투표 방지 |
| AI 피드백 | Anthropic Claude API (claude-sonnet-4-6) | 플랜 B 예시 생성 |

### 배포

| 항목 | 선택 |
|---|---|
| 호스팅 | Vercel 또는 Firebase Hosting |
| 도메인 | 단기 연수용: 짧은 URL + QR 생성 (예: `bit.ly/...`) |

---

## 7. 개발 우선순위 (단계별 구현)

### Phase 1 — MVP (연수 직전 투입 가능 수준)

- [ ] 입장 화면 (이름 + 세션코드 입력)
- [ ] 실패 이력서 작성 파트 1~3 (로컬 저장)
- [ ] 관리자 세션 생성 화면
- [ ] PDF 내보내기 (개인 이력서)

### Phase 2 — 실시간 공유

- [ ] Firebase 연동 (실시간 DB)
- [ ] 모둠 공유 보드 (S-03)
- [ ] 공감 버튼 + 한 줄 댓글
- [ ] 원인 카테고리 분포 차트

### Phase 3 — 투표 시스템 (핵심)

- [ ] 관리자 "투표 오픈" 버튼 및 제어판 (A-01)
- [ ] 참가자 투표 화면 (S-04) — 카테고리별 1명 선택
- [ ] 중복 투표 방지 (익명 UID 기반)
- [ ] 실시간 투표 참여율 모니터링 (A-02)
- [ ] 투표 마감 + 결과 집계

### Phase 4 — 시상식 연출

- [ ] 결과 공개 화면 (S-05 / A-03) — 카테고리별 순차 공개
- [ ] 대형 화면 투사 모드 (전체 화면, 최소 UI)
- [ ] 수상자 이력서 풀스크린 공개
- [ ] Claude API 연동 — 전체 세션 인사이트 요약 리포트 생성

---

## 8. UX 설계 원칙

### 모바일 퍼스트

모든 화면은 375px 기준으로 설계. 태블릿·PC는 중앙 정렬 카드 레이아웃으로 자동 적용.

### 진행 상태 항상 표시

화면 상단에 현재 단계 표시 (이력서 작성 → 공유 → 투표 → 결과). 참가자가 "지금 무엇을 해야 하는가"를 즉시 인지할 수 있어야 함.

### 투표 잠금 메커니즘

관리자가 투표를 오픈하기 전 S-04 화면은 잠금 상태 표시("퍼실리테이터가 투표를 곧 오픈합니다"). 오픈 시 자동으로 활성화되며 참가자가 별도로 새로고침할 필요 없음 (실시간 상태 동기화).

### 자기 자신 투표 방지

투표 화면에서 본인의 카드는 선택 불가 처리 (흐림 처리 + 안내 문구).

### 익명성 보장

투표 결과는 관리자가 "공개" 버튼을 클릭할 때까지 참가자에게 노출되지 않음. 투표 집계 시 개인 식별 정보와 선택 내용은 분리 저장.

---

## 9. 보안 및 운영 고려사항

| 항목 | 방안 |
|---|---|
| 중복 투표 | Firebase 익명 UID 기반 1인 1투표 강제 |
| 세션 접근 제어 | 세션코드 유효성 검증, 마감된 세션 차단 |
| 개인정보 | 이름은 닉네임 허용, 서버 저장 최소화, 세션 종료 24시간 후 자동 삭제 옵션 |
| Claude API 비용 | 플랜 B 피드백은 선택적 기능 (버튼 클릭 시만 호출), 세션당 호출 횟수 제한 설정 |
| 오프라인 대응 | 이력서 작성 내용은 localStorage 동시 백업 (네트워크 끊김 시에도 작성 유지) |

---

## 10. 화면 흐름 요약

```
[참가자]
  입장 (S-01)
    → 실패 이력서 작성 (S-02: 파트1 → 파트2 → 파트3)
      → 모둠 공유 보드 (S-03) ← 관리자가 공유 단계 오픈 시
        → 투표 대기 화면 ← 관리자 투표 오픈 전
          → 시상 투표 (S-04) ← 관리자 투표 오픈 시 자동 전환
            → 투표 완료 대기
              → 시상 결과 (S-05) ← 관리자 결과 공개 시 자동 전환

[관리자]
  관리자 대시보드 (A-01)
    → 세션 생성 / 단계 제어
      → 모니터링 (A-02): 작성율 · 투표율 실시간 확인
        → 투표 오픈 버튼
          → 투표 마감 버튼
            → 결과 공개 (카테고리별 순차)
              → 대형 화면 투사 모드 (A-03)
```

---

## 부록 A. 시상 카테고리 기본 설명문

| 카테고리 | 시상 문구 |
|---|---|
| 올해의 아름다운 폭망상 | "가장 용감하게 도전했고, 가장 화려하게 실패했으나, 그 용기 자체를 기립니다." |
| 불사조상 | "멘붕의 순간, 아무도 예상 못한 방법으로 수업을 구해낸 그 순발력에 경의를 표합니다." |
| 인간 디버거상 | "오류와의 처절한 사투 속에서도 포기하지 않은 끈기, 그 자체가 훈장입니다." |

---

## 부록 B. 연수 현장 운영 팁

- 관리자 계정은 연수 시작 전 미리 로그인 후 대시보드 대기
- 세션 QR코드는 강의장 입구 및 PPT 첫 슬라이드에 동시 노출
- 이력서 작성 단계에서 강사가 먼저 본인의 실패 사례를 화면에 입력하는 시범을 보이면 참여율 향상
- 투표 오픈 타이밍은 모둠 공유 토론이 충분히 이루어진 후 (최소 5분 이상 공유 후 오픈 권장)
- 시상 결과 공개는 카테고리당 10~15초 텀을 두고 순차 공개하여 극적 효과 극대화
- 세션 종료 후 관리자는 전체 이력서 데이터를 CSV로 내보내 후속 연수 자료로 활용 가능

---

*최종 수정: 2025년 6월 | 삼현여자중학교 AI 융합교육 연구*
